# Моделирование движения материального тела и полей скоростей

Проект реализует численное моделирование движения материального тела и полей скоростей в плоскости на основе заданных скоростных функций и схемы Рунге–Кутты 3-го порядка. Код позволяет:

- задавать геометрию материального тела (набор материальных точек);
- численно рассчитывать траектории материальных точек;
- строить сетку точек пространства и вычислять поле скоростей;
- визуализировать:
  - траектории материальных точек;
  - поля скоростей во времени;
  - начальную и деформированную форму тела (в третьей четверти).

---

## 📌 Основная идея

Рассматривается плоское движение в переменном во времени поле скоростей:

- для координаты `x(t)`:
  \- dx/dt = func_x(t, x) = −exp(t) · x  
- для координаты `y(t)`:
  \- dy/dt = func_y(t, y) = exp(t) · y  

Система дифференциальных уравнений интегрируется с помощью явного метода Рунге–Кутты 3-го порядка (задаётся коэффициентами матриц `a`, `b`, `c`), реализованного в функции `runge_method`.

---

## 📂 Структура проекта

### Папка `actions`

**`funcs.py`**

- `create_mb(x_0, y_0, r1, n_points)`  
  Создаёт объект `MaterialBody`, формируя набор материальных точек на дуге окружности радиуса `r1` в третьей четверти (углы от π до 3π/2).

- `move_mb(time, h, mb, a, b, c)`  
  Строит объект `BodyTrajectory`. Для каждой материальной точки интегрирует её движение по времени с шагом `h` на интервале `time` с использованием метода Рунге–Кутты (`runge_method` и функции `func_x`, `func_y`).

- `move_ts(time, h)`  
  Формирует последовательность полей скоростей во времени.  
  Для каждого момента времени строится равномерная сетка точек в квадрате [-5, 5]×[-5, 5], и в каждой точке вычисляются значения скоростей `vx`, `vy`.  
  Возвращает список объектов типа `SpaceGrid`.

**`plotting.py`**

Функции для визуализации результатов:

- `plot_trajectory(mb, tr)`  
  Строит:
  - исходное расположение материальных точек (красные точки),
  - траектории движения (синие линии),
  - конечное положение точек (зелёные точки).  
  Сохраняет рисунок в файл `assets/plot_trajectory.svg`.

- `plot_velocity_fields(vf)`  
  Для набора полей скоростей `vf` по времени:
  - строит векторные поля скоростей (`quiver`);
  - дополнительно рисует набор кривых вида `y = c * x^d` для визуализации поведения решений и границ;  
  - сохраняет каждый рисунок в файл `assets/velocity_fieldsN.svg`.

- `plot_initial_and_deformed(material_body, body_trajectory, t_index=-1)`  
  Сравнивает начальное и деформированное положение тела:
  - начальные координаты материальных точек;
  - координаты в момент времени с индексом `t_index` (по умолчанию последний шаг).  
  Сохраняет рисунок в `assets/initial_and_deformed_form.svg`.

---

### Папка `entities`

**`classfile.py`**

Классы модели:

- `MaterialPoint`  
  Описывает материальную точку:
  - `coord_x`, `coord_y` — текущие координаты;
  - `x_0`, `y_0` — начальные координаты.

- `MaterialBody`  
  Набор материальных точек, представляющих тело:
  - `setBody(x_0, y_0, r1, n_points)` — задаёт точки на дуге окружности радиуса `r1`, центр в `(x_0, y_0)`, углы от π до 3π/2 (третья четверть).

- `PointTrajectory`  
  Хранит траекторию отдельной материальной точки:
  - ссылка на `MaterialPoint`;
  - массивы координат `x(t)` и `y(t)`.

- `BodyTrajectory`  
  Описывает движение всего тела:
  - ссылка на `MaterialBody`;
  - список `PointTrajectory` для всех точек тела;
  - метод `setBodyTrajectory(time, h, mb, a, b, c)` запускает численное интегрирование движения всех точек.

- `SpacePoint`  
  Точка пространства с полем скоростей:
  - координаты `coord_x`, `coord_y`;
  - компоненты скорости `velocity_x`, `velocity_y`;
  - время `t`.

- `SpaceGrid`  
  Сетка пространственных точек (поле скоростей в один момент времени):  
  - список `space_points`.

**`constants.py`**

Содержит параметры задачи и коэффициенты метода:

- начальные параметры тела:
  - `x_0 = y_0 = -1` — положение центра;
  - `r1 = 4` — радиус;
  - `n_points = 20` — число материальных точек;

- параметры интегрирования:
  - `time = 1` — конечное время моделирования;
  - `h = 0.01` — шаг по времени;

- коэффициенты таблицы Рунге–Кутты (`a`, `b`, `c`) для 3-стадийной схемы.

**`math_utils.py`**

Математические функции:

- `func_x(t, x)` — правая часть уравнения для x: `dx/dt = -exp(t) * x`
- `func_y(t, y)` — правая часть уравнения для y: `dy/dt = exp(t) * y`
- `runge_method(x_0, h, n, func, a, b, c)` — реализация обобщённого явного метода Рунге–Кутты 3-го порядка:
  - `x_0` — начальное значение;
  - `h` — шаг;
  - `n` — число шагов;
  - `func` — правая часть (например, `func_x` или `func_y`);
  - `a`, `b`, `c` — коэффициенты таблицы Рунге–Кутты.

Возвращает список значений `x(t)` на всех временных шагах.

---

## 📊 Результаты и визуализация

В процессе работы модули `plotting.py` создают SVG-файлы в папке `assets`:

- `plot_trajectory.svg` — траектории материальных точек;
- `velocity_fieldsN.svg` — последовательность полей скоростей во времени;
- `initial_and_deformed_form.svg` — сравнение начальной и деформированной формы тела.

---

## 🔧 Зависимости

Для работы проекта необходимы:

- Python 3.x
- numpy
- matplotlib

Установка (через pip):

pip install numpy matplotlib

---

## 📘 Лицензия

Тип лицензии можно указать по выбору (например, MIT). Если лицензия ещё не выбрана, рекомендуется добавить файл LICENSE в корень репозитория.

---

## 🤝 Авторы и сотрудничество

Проект разработан совместно двумя авторами.  
Pull-requests, вопросы и предложения по улучшению кода и модели приветствуются.
